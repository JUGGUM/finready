package io.finready.coverage;

/** coverage_result.provenance_failure_reason — ck_provenance_failure_reason */
public enum ProvenanceFailureReason {
	EMPTY,
	TOO_SHORT,
	TOO_LONG,
	NOT_FOUND,
	AMBIGUOUS
}
